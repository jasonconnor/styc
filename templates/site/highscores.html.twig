{% extends 'inc/base.html.twig' %}

{% block title %}Highscores{% endblock %}

{% block main %}

    <h1>Highscores</h1>

    <table>
        <tr>
            <th>Username</th>
            <th>Score</th>
            <th>Date</th>
        </tr>

        {% for score in scores %}

            <tr>
                <td>User</td>
                <td>{{ score.score }}</td>
                <td>Now</td>
            </tr>

        {% endfor %}
    </table>

{% endblock %}